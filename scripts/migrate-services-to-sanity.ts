/**
 * Roofing seed data was removed from this template.
 * Implement pages from Figma, then migrate that content into Sanity if needed.
 */
console.error('Local roofing seed files were removed. This migration script is no longer used.');
process.exit(1);
