/**
 * Seed Contact and Reviews page singletons into Sanity.
 *
 * Local seed files for previous clients were removed. Implement pages from
 * Figma, then migrate that content into Sanity if needed.
 */
console.error('Client seed files were removed. This migration script is no longer used.');
process.exit(1);
